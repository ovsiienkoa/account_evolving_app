
#TODO
the same model is being initialized in bq models and here separately, could be a bug creator in future

currently user can trick llm into selecting a different user_id
currently agent can plot only 2d plots (in terms of fields number), needs to be able to plot 3d plots and more
currently in history all user requests and only last agent response is being logged
_gather_information should be a shared function